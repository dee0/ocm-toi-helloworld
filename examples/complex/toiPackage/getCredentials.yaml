getCredentials: (( lambda |x,y|->eval("_.requestedCredentials." x "." y) )) 

# Everything below is for mocking the OCM getCredentials 
requestedCredentials: 
  myExecutorOCI:
    user: jdoeName
    password: passw0rd
